import {  withRouter } from "react-router";
import { AppWrapper } from "../../components/AppWrapper";

const AppWrapperContainer = withRouter(AppWrapper);
export { AppWrapperContainer as AppWrapper };
